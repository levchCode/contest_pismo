version: '3'
services:

  server:
    build: 
      context: .
      dockerfile: server.dockerfile
    env_file: ./.env
    ports:
      - "5000:5000"
    depends_on: 
      - mongo
  mongo:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - db_data:/data/db

volumes:
  db_data: